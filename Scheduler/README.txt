Make file build the uses the src and include directories for .cpp and .h files respectively.

It creates an executable file called sched.out (also attached for reference). 

Just run 'make' and the sched.out will be overwritten and ready to use with runit.sh and gradeit.sh
 